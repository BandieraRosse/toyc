# Rasterfall GPU RB-0 最新交接

## 最新交接：2026-09-22 最终验证与文档签收完成，优先于下方全部旧记录

用户要求的最终验证和文档签收已完成。首次 Full 在 thin-far 稳定发现 SKY validator 把合法
`direction=(-1024,0)` 误判为无效；现已修正为 yaw/pitch 的 sin/cos 各自不能同时为零，并补 pack 单元回归
与 Vulkan/mixed preflight 失败诊断。没有改变提交顺序、同步或 Graphics 类型。

最终 Windows package：14,375,420 bytes，SHA-256
`F407FD19BFC1FBC049ADE78EA21EB9E71D7CD2B627C0CAD388CB1DC8E363D762`。最终证据：

- `tmp/rb0-final-full2-20260922`：Full 31/31 PASS。
- `tmp/rb0-final-special-20260922`：All PASS；validation/sync、五类 fault、10,000 帧 soak 均通过。
- VUID/SYNC-HAZARD/ERROR 零命中；零 fallback/readback/CPU copy/hot queue-idle。
- 平衡电源方案，Intel Iris Xe，GPU 严格串行；结束后无遗留进程。

Intel 单设备 RB-0 已完成实现、归因、最终回归和文档签收，阶段正式结束。fog、unclassified 和历史双态
按既定结论冻结；RTX 3050/第二物理 GPU 继续按用户要求暂缓，所以不称跨设备签收，也不自动进入 RB-1。
用户已于 2026-09-22 确认结束 RB-0；下方旧记录仅保留过程现场。

## 最新交接：2026-09-22 性能归因完成，优先于下方全部旧记录

已按最新要求完成 schema 5 离线性能归因；没有修改同步、producer、bridge 或 Graphics，也没有重新打开 fog。
结论与完整表格已写入 `rasterfall/docs/gpu-rb0-special-20260922.md` 的“schema 5 性能归因结论”。

低扰动五轮最慢 5% 共 170 帧：near0 为 CPU producer 1 / unclassified 29；near30 为 CPU producer 3 /
GPU Raster 21 / unclassified 6；near60 为 CPU producer 19 / GPU Raster 6 / unclassified 5；Campaign 为
CPU producer 5 / GPU Raster 60 / unclassified 15。55 个 unclassified 的四个互斥顶层 phase 均闭合 whole-loop，
缺的是 phase 内因果计时。near60 五个未分类样本为四个 render 主导、一个 prepare 主导；最强已测信号均为
CPU producer，但未达到半帧阈值，保持 unclassified。

历史 near60 数百毫秒跨轮双态在正确口径五轮中未复现；当前 P95 45.258--54.805 ms、P99
47.806--59.508 ms，只有单帧最大 76.709 ms。固定 workload/bridge、timestamp 截断、slot wait、
present/acquire 和单一 graphics fence wait 均不能解释历史双态；低扰动 phase 内调度、OS/驱动抢占、
频率/温度仍未实测。以“当前正确口径未复现、历史根因未知的已知风险”冻结，不能称根因闭合。

注意正式五轮 manifest 的 package SHA 是 `E932...AD1`；SKY/overlay 修复后的最终 package 是
`4A0F...BD0`，文档已消除原“同一 package”歧义。下一步剩余：第二物理 GPU 继续暂缓；执行最终
Full/专项回归、文档签收，并由用户决定是否提交。仍严格停留 RB-0。

## 最新交接：2026-09-22 12:55，优先于下方全部旧记录

用户最新决定：**fog 暂不实现并冻结；下一会话先完成性能归因。** fog/远墙明暗差异作为已知、
已定位且明确接受的冻结项记录，不再作为当前性能归因工作的阻点；不要借此扩展 Graphics，仍不进入 RB-1。

总体进度：RB-0 的测量可信度、actor 命令范围、timestamp 容量/关联、七类 submit/wait、raw 分段输入
生命周期和 Graphics shared-color resize 生命周期均已修复；validation/sync、五类 fault、10,000 帧 soak、
Quick/Full、审计/低扰动各五轮均已有通过证据。随后又补齐 mixed SKY 与 overlay 血条 coverage，Windows
build/package、完整 logic test、同 tick near/mid 重采和 GPU Quick 再次通过。仍未提交，不进入 RB-1，
不优化同步、不合并 span、不跳 bridge、不迁移 producer、不跑 update-bootstrap。

当前最终 package：`build-windows/rasterfall-windows/rasterfall.exe`，14,373,884 bytes，SHA256
`4A0FDDBA36B7150B657DBBECB8971F9A1621EDBE4BB168BDEBEB21A6B3C15BD0`。最新画面证据在
`tmp/rb0-visual-fix-20260922`，最新 Quick 在 `tmp/rb0-visual-fix-quick-20260922`。退出时无遗留
Rasterfall/GPU 测试进程；工作区全部既有修改必须保留。

下一会话唯一主线是性能归因：

1. 先读根 `AGENTS.md`、`rasterfall/docs/README.md`、
   `rasterfall/docs/gpu-rb0-special-20260922.md`，再核对 `git status --short`。
2. 只使用 schema 5 和当前最终 package 的证据。有效正式五轮为
   `tmp/rb0-special-audit5-20260922` 与 `tmp/rb0-special-noaudit5-20260922`；旧组 workload 错误且
   whole-loop 口径不同，不得直接计算“优化收益”或套用旧慢帧分类。
3. 目标是把当前 `unclassified` 慢帧进一步归因，并判断历史 near60 数百毫秒跨轮双态在正确口径下
   是否可复现、可由哪一条实测信号解释。慢帧分类只是启发式，不是因果证明；不能把当前 wait 与
   前序 N-1 GPU timing 当作逐 submit 因果分摊，也不能继续使用外部日志间隔推断本帧原因。
4. 优先离线复核现有逐帧 schema 5 数据和 `tmp/rb0-special-analysis.json` / `tmp/rb0-analyze.py`；若现有
   字段不足，再做最小测量补强。不要先改同步、producer 或 bridge，也不要因未复现双态而直接宣布根因闭合。
5. 若必须重新采样：Windows 原生 PowerShell、平衡电源方案、ACLineStatus=1、1280x720、fixed tick、
   GPU 严格串行；先确认无遗留进程，审计与低扰动分开解释。RTX 3050/第二物理 GPU 仍暂缓。
6. 交付应明确列出：已测信号、仍缺失的时间、每类慢帧样本数、可证伪的候选原因、历史双态是否复现，
   以及能否以“当前正确口径未复现、根因未知的已知风险”冻结。没有证据时保持 `unclassified`。

性能归因完成后仍剩：第二物理 GPU 验证（继续暂缓），以及最终 Full/专项回归、文档签收和是否提交。
fog 已冻结，不要重新打开，除非用户明确改变决定。

---

## 最新交接：2026-09-22 12:49，优先于下方全部旧记录

继续严格停留在 RB-0；未提交，不进入 RB-1，不扩展 Graphics，RTX 3050 仍暂缓。
在 12:13 交接之后已闭合两项旧画面缺口：mixed frame 冻结 Core SKY 参数并生成既有 `SKY_V1`，
overlay 私有矩形改走 coverage-aware `fb_fill_rect()`，世界名字下的血条和 BASE 条形可被 native composite 消费。
Windows build/package、完整 logic test 与 GPU Quick 通过；Quick 证据为
`tmp/rb0-visual-fix-quick-20260922`。最终 package 为 14,373,884 bytes，SHA256
`4A0FDDBA36B7150B657DBBECB8971F9A1621EDBE4BB168BDEBEB21A6B3C15BD0`。

同 fixed tick、第 30 帧 near/mid CPU/native 重采位于 `tmp/rb0-visual-fix-20260922`，四次退出码均为 0，
无遗留进程。near 天空主体逐像素零差异，near/mid 的名字、血条和 BASE 条形已人工确认恢复。
远墙仍比 CPU 明亮，原因已经定位：CPU planar 路径应用 camera-dependent `baked_fog_at()`，
persistent-map Graphics Draw 没有等价 fog 参数。RB-0/RB-1 前不得为此扩大 Graphics；不要把完整
CPU/native 画面对照或 RB-0 标为通过。下一步仍需处理部分 unclassified 慢帧、历史双态因果和
第二物理 GPU（后者继续按用户要求暂缓）；若继续改 runtime，再按风险重跑专项/Full。

---

## 最新交接：2026-09-22 12:13，优先于下方全部旧记录

用户要求快速收尾。本轮已完成专项修复与新五轮；**未提交，RB-0 仍未签收，不进入 RB-1。**
先读根 `AGENTS.md`、`rasterfall/docs/README.md` 和
[本轮专项修复、证据与新基线](rasterfall/docs/gpu-rb0-special-20260922.md)。
11:30 交接及更早记录仅供追溯，不能继续按“raw memcmp 尚未修复/新五轮尚未运行”处理。

HEAD 仍为 `79b405da6312abd73b3eaea5e78c7cb12a784f01`。先 `git status --short`，保留全部已有修改。
严格 RB-0：不优化同步、不合并 span、不跳 bridge、不迁移 producer、不扩展 Graphics、不提交，
不跑 update-bootstrap。Windows 原生 PowerShell，平衡电源方案，GPU 严格串行，RTX 3050 继续暂缓。

本轮新增修复：

- raw 分段最终才提交，却反复覆盖相同上传缓冲；较早 CLEAR 读到后续毒化值，像素 0 从期望
  `ff102030` 变为 `ffabcdef`。现在保留 command/bin/texture 输入版本至安全释放，原精确断言完整保留。
- 真正启用 validation 后暴露 Graphics resize 的旧 image/view 悬空；该次 rf-gpu 异常弹窗由用户终止，
  明确记为失败。现在维护 shared-color 借用关系，resize 废弃旧录制、重绑定匹配 extent，销毁时解除关系。
  新增 resize 后 LOAD/Draw 拒绝及先销毁 Graphics 后重启 Raster 的回归；之后 Core/sync validation 通过。
- 新增 `tools/gpu_rb0_special.ps1`，可分 Validation/Faults/Soak；修正本地 layer DLL 的依赖 PATH，
  必须证明 Khronos layer 加载且 Synchronization 开启。record/submit fault 正确退出码为 3；
  present-out-of-date 的故障帧 poisoned 必须在下一帧新 generation 恢复。
- `tools/gpu_metrics.ps1` schema 5 接入七类 wait、互斥 CPU phases 与前序 GPU frame；不再将
  外部日志间隔当成本帧原因。sampling 增加 Win32 AC 状态、驱动 DLL 证据与运行边界电源/package 复核。

最终 package：`build-windows/rasterfall-windows/rasterfall.exe`，14,373,168 bytes，SHA256
`E932E59366F058F315DDFCB3EB1284BA3A8CF5A26FB252A19CEA0F15BAE76AD1`。
构建/package 已完成；所有本轮门禁与正式采样使用该 package。退出时无遗留 GPU 测试/游戏进程。

已通过证据：

- raw 精确比较、截断/扩容和生命周期 regression：`tmp/rb0-segment-final-validation.out/.err`。
- native resize 与 300 帧 validation：`tmp/rb0-special-final-20260922`；该目录后续因脚本错误预期退出码停止，
  不把整个旧 manifest 标为 PASS。修正后的五类 fault：`tmp/rb0-special-faults2-20260922`，PASS。
- 10,000 帧 soak：`tmp/rb0-special-soak-20260922`，PASS，零 fallback/readback/CPU copy。
- Quick：`tmp/rb0-special-quick-20260922`；Full：`tmp/rb0-special-full-20260922`，均 PASS，Full 31 项。
- 审计五轮：`tmp/rb0-special-audit5-20260922`；低扰动五轮：`tmp/rb0-special-noaudit5-20260922`，
  各 20 次运行全部通过，四场景逐帧 workload hash 跨轮一致。
- timestamp 两组均无截断；每次末尾两帧未回收，未计入 GPU 分位数。
- Intel 实际 loader DLL 文件版本 `32.0.101.6314`；Win32 ACLineStatus=1。CIM 仍不可用，但不再笼统写
  driver/供电来源完全未知；路径与证据见专项报告和 sampling manifest。

低扰动 near60：whole median 38.388–43.896 ms，中位轮 40.296；P95 45.258–54.805，
P99 47.806–59.508。本组未复现旧数百毫秒双态；旧 workload/计时口径错误，不能算作优化收益或声称根因闭合。
near60 body opaque 命令中位 26,549，weapon 1,528，gear 768；正常同步提交为每帧一次 skinning，
bridge caller 为零；前序关系为 N-1，但不等于逐 submit 因果分摊。更多统计见报告。

**剩余阻点和下一步**：

- 同 fixed tick 的 near/mid CPU/native 图已采集并人工查看：
  `tmp/rb0-special-visual-20260922/comparison.png`。native 天空深色、CPU 天空渐变，世界血条和远墙明暗也有差异。
  上轮 native 截图已有相同现象；本轮保持不改变画面语义，没有修这些旧缺口。不能称完整画面对照通过。
  mixed executor 临时 renderer pack 未附加 SKY 参数；血条/围墙原因未闭合。先读报告，确定后续处理范围。
- 部分慢帧仍 unclassified，历史双态因果未闭合；第二物理 GPU/RTX 3050 仍暂缓。
- 本轮未做 RB-1 优化。不要因为已有新性能数字就直接进入 RB-1。
- 工作区新增报告 `rasterfall/docs/gpu-rb0-special-20260922.md`、专项脚本及相关源码/工具/导航修改；
  上轮所有修改仍保留。`git diff --check` 通过，仅既有 LF/CRLF 提示。不要提交 tmp/build 证据。

---
以下为旧交接，保留作历史参考；如与上文冲突，以上文及当前实际输出为准。

# Rasterfall GPU RB-0 新会话交接

## 最新交接：2026-09-22 11:30，优先于下方旧记录

用户要求快速收尾，下一会话继续执行。**本轮已修改运行时代码；未提交；未重跑正式五轮；RB-0 未完成。**

先读根 AGENTS.md、rasterfall/docs/README.md、
[本轮修复与续接](rasterfall/docs/gpu-rb0-repair-20260922.md)、
[原调查报告](rasterfall/docs/gpu-rb0-investigation-20260922.md)，再读 GPU plan/architecture/current-state/windows-native。
下方旧交接中的 bridge fence、external_scheduler 和旧 workload 结论已被调查报告修正。

HEAD 仍为 `79b405da6312abd73b3eaea5e78c7cb12a784f01`。先 `git status --short`，保留全部修改。
严格 RB-0：不优化同步、不合并 span、不跳 bridge、不迁移 producer、不扩展 Graphics、不提交、
不跑 update-bootstrap。Windows PowerShell 签收，平衡电源方案，RTX 3050 暂缓，GPU 进程严格串行。

已修复：

- actor 裁剪改为 flush 前 scope callback，旧起点不会跨缓冲使用；内容/顺序回归通过。
- timestamp 按 mixed 执行计划动态预留，显式 requested/recorded/dropped，截断 valid=0；仍按 GPU frame 回填。
- 七类实际 gfx_submit 调用者 submit/wait，关联提交时前序未确认完成的 Raster frame。
- 低扰动 whole_us 与 audit 统一到 whole-loop 起点；互斥 prepare/render/execute/remainder；
  fallback reason 改为 unclassified，unattributed 仍不能当 CPU 余项。
- sampling/metrics/acceptance 增加 timestamp 覆盖门禁及 package SHA。

最终 package：`build-windows/rasterfall-windows/rasterfall.exe`，14,371,092 bytes，2026-09-22 11:20:56，
SHA256 `99E1932B9C2F8C21389D6A9AF7CE8C6E7F67AE082372B535BEA9DB739085D374`。
Windows build/package、logic、Quick `tmp/rb0-repair-quick3-20260922`、Full
`tmp/rb0-repair-full-20260922` PASS；Full 31 条运行。native 八 Draw fixture 完整覆盖 36 区间。
near60 首帧 body=24364、gear=769、weapon=1560；旧 weapon=97456 模式消失。

**当前下一步阻点**：专项脚本 `tmp/rb0-special.ps1` 在 raw `rf-gpu-raster-test.exe --mixed-gate`
的 segmented color memcmp（源码第 206 行）失败，尚未运行新增截断测试/validation/fault/soak。
证据 `tmp/rb0-special-20260922-112733`。HEAD 原 backend/graphics/test 的隔离重编译也同处失败：
`tmp/rb0-baseline-raster-result.log`，根因未查明；不要删掉/放宽断言。
该脚本已准备本地 validation layer、五类故障、10000 帧 soak，尚未验证后续分支，需实际执行并核对日志。

后续顺序：先处理专项失败并完成相关门禁；如修改 runtime 则重新 build/package、Quick/Full；
然后对同一最终 package 分开运行 `tools/gpu_rb0_sampling.ps1 -Rounds 5` 和 `-Rounds 5 -NoAudit`。
补画面与实际命令内容复核，分析真实 producer 成本、完整 timestamp、N 帧 wait 与前序帧的关系。
旧 workload 错误且 whole_us 口径不同，不能直接算新旧“优化收益”。

仍未覆盖：新五轮、双态原因闭合、专项验证、完整人工 CPU/reference 组图、Intel driver/供电来源、
第二物理 GPU。metrics 的旧慢帧 classifier 尚未接入七类 wait 和新 CPU phase，详见修复报告。

以下为上一会话旧记录，保留作为历史，不作为本轮当前状态。

  继续 Rasterfall GPU RB-0 开发。开始前必须阅读：

  - rasterfall/docs/README.md
  - rasterfall/docs/gpu-rendering-architecture.md
  - rasterfall/docs/gpu-raster-bridge-plan.md
  - rasterfall/docs/gpu-current-state.md
  - rasterfall/docs/windows-native-codex.md

  严格停留在 RB-0：

  - 不进入 RB-1。
  - 不扩展 Graphics 类型。
  - 不迁移 producer。
  - 不改变画面、层顺序或提交顺序。
  - 不合并 span，不跳过 bridge。
  - 保留全部现有工作区修改。
  - 不格式化或覆盖无关文件。
  - 不提交 build/、tmp/ 或采样证据。
  - 不运行 make update-bootstrap。
  - GPU 实例必须严格串行。
  - RTX 3050 继续暂缓。
  - Windows 原生 PowerShell 是唯一签收 lane。
  - 活动电源方案使用“平衡”，不要切换至高性能；当前环境不允许高性能方案的风扇噪声。

  当前 HEAD：

  79b405da6312abd73b3eaea5e78c7cb12a784f01

  ## 一、当前工作区

  原有修改继续保留，并新增两个 RB-0 wait 统计文件的修改。

  当前已修改：

  gpu/include/rf_gpu_mixed_executor.h
  gpu/src/rf_gpu_mixed_executor.c
  rasterfall/docs/README.md
  rasterfall/docs/gpu-current-state.md
  rasterfall/docs/gpu-raster-bridge-plan.md
  rasterfall/docs/gpu-rendering-architecture.md
  rasterfall/include/rasterfall_options.h
  rasterfall/include/rf_core_host.h
  rasterfall/include/rf_core_mixed_frame.h
  rasterfall/src/dev-tests/rf_core_mixed_frame_test.inc
  rasterfall/src/rasterfall_options.c
  rasterfall/src/rasterfall_render.c
  rasterfall/src/render/rasterfall_enemy_rig.inc
  rasterfall/src/rf_core_host.c
  rasterfall/src/rf_core_mixed_frame.inc
  rasterfall/src/rf_game_runtime.c
  tools/gpu_acceptance.ps1
  tools/gpu_metrics.ps1

  未跟踪：

  tools/gpu_rb0_sampling.ps1

  最终 git diff --check 已通过，仅有既有 LF/CRLF 转换提示。

  不要撤销、覆盖或重新格式化这些修改。

  ## 二、本轮完成的平衡方案五轮采样

  正式有效证据目录：

  tmp/gpu-rb0-sampling-20260922-102453

  运行命令：

  powershell -ExecutionPolicy Bypass -File tools/gpu_rb0_sampling.ps1 `
    -Rounds 5 -NoAudit

  采样条件：

  - 活动电源方案：平衡
  - GUID：381b4222-f694-41f0-9685-ff5bb260df2e
  - commit：79b405da6312abd73b3eaea5e78c7cb12a784f01
  - package exe 长度：14,363,168 bytes
  - package exe 时间：2026-09-22T09:41:20.1978233+08:00
  - adapter：Intel Iris Xe
  - path：gpu-native
  - extent：1280×720
  - fixed simulation tick：已确认
  - CIM adapter/driver 查询：权限拒绝
  - driver version：仍未覆盖
  - 20/20 次运行成功
  - 20 条 RB0-STATS
  - 170 条 RB0-SLOW
  - GPU 实例严格串行
  - 结束后无遗留 rasterfall.exe

  平衡方案预热 16 帧后的结果：

   场景                Median 范围 / 中位轮              P95 范围              P99 范围
  ━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━
   near 0         19.296–20.004 / 19.903 ms      21.120–24.859 ms      22.869–54.032 ms
  ──────────  ──────────────────────────────  ────────────────────  ────────────────────
   near 30       94.355–100.644 / 97.359 ms    111.893–188.420 ms    118.232–227.479 ms
  ──────────  ──────────────────────────────  ────────────────────  ────────────────────
   near 60     133.752–156.078 / 148.916 ms    171.969–442.528 ms    192.907–450.468 ms
  ──────────  ──────────────────────────────  ────────────────────  ────────────────────
   Campaign       25.728–26.021 / 25.902 ms      33.937–68.321 ms      38.283–73.252 ms

  平衡组慢帧旧分类：

  - near 0：
      - external_scheduler：29
      - cpu_producer：1

  - near 30：
      - slot_or_fence_wait：25
      - external_scheduler：4
      - raster_workload：1

  - near 60：
      - slot_or_fence_wait：27
      - external_scheduler：3

  - Campaign：
      - external_scheduler：46
      - slot_or_fence_wait：34

  Bridge：

  - near 0/30/60：固定 12 次、88,473,600 bytes
  - Campaign：
      - 14 次、103,219,200 bytes：9 个慢帧
      - 16 次、117,964,800 bytes：71 个慢帧

  字段完整性：

  - reason：全部存在
  - attributed_us：全部存在
  - gpu_raster_us：全部存在

  ## 三、与节能组的对照结论

  此前节能方案正式五轮目录：

  tmp/gpu-rb0-sampling-20260922-100221

  节能方案 near 60：

  - median：146.750–203.240 ms，中位轮 157.118 ms
  - P95：174.307–583.242 ms
  - P99：178.815–641.747 ms
  - 慢帧：
      - slot_or_fence_wait：18
      - external_scheduler：12

  平衡方案整体 median 更低，near 60 长尾上界也降低，但双态仍然明显存在：

  - 正常轮 near 60 P95 约 172–240 ms
  - 异常轮 near 60 P95 约 340–443 ms

  因此已经确定：

  - 电源方案影响整体吞吐和长尾幅度。
  - “节能方案单独造成 near 60 双态”的假设不成立。
  - 平衡方案应作为后续正式采样条件。
  - near 60 双态不是 workload 漂移。
  - near 60 双态不是 bridge 次数或字节漂移。
  - 不能据此进入 RB-1。

  ## 四、一组明确无效的采样

  目录：

  tmp/gpu-rb0-sampling-20260922-102003

  这组在“高性能”方案下启动，但用户因风扇噪声在运行中切换到“平衡”。

  脚本不响应控制台中断，且 CIM 父进程查询因权限拒绝不可用，因此为避免宽泛终止 PowerShell，最终让该组严格串行自然结束。

  虽然脚本显示 PASS，但电源方案在运行中变化，不满足固定采样条件：

  - 不得用于任何性能比较。
  - 不得与节能组或平衡正式组拼接。
  - 文档已经记录该组无效。

  ## 五、本轮发现的旧统计问题

  旧 RB0-SLOW 分类中：

  slot_or_fence_wait

  实际只使用：

  gpu_audit.mixed_graphics_wait_ms

  该字段来自 graphics bridge 路径的 fence wait。

  它并不包含独立的 frame-slot recycle wait。因此旧名称把多个可能含义混在了一起。

  此外：

  - GPU timestamp 来自双帧 slot 延迟完成。
  - 旧实现将刚返回的 GPU timestamp 直接放入当前 CPU frame 样本。
  - 实测 GPU timestamp 通常落后当前 CPU frame 两帧。
  - 旧 reason 没有把 gpu_raster_us 作为候选。
  - 旧输出没有 GPU Raster/Draw/bridge 的完整分位数。
  - 未输出未覆盖时间。

  这些问题使旧 external_scheduler 和 slot_or_fence_wait 不能直接作为最终原因。

  ## 六、本轮实施的新 RB-0 统计

  ### 1. Frame-slot recycle wait

  在：

  gpu/src/rf_gpu_mixed_executor.c

  围绕：

  rf_gpu_vulkan_raster_recycle(...)

  增加独立计时。

  累计字段位于：

  gpu/include/rf_gpu_mixed_executor.h

  字段：

  slot_wait_ms

  Core snapshot 中对应：

  mixed_slot_wait_ms

  涉及：

  rasterfall/include/rf_core_host.h
  rasterfall/src/rf_core_host.c

  这只测量调用，不改变 recycle、wait 或提交顺序。

  ### 2. Wait 分类细分

  低扰动样本现在区分：

  - slot_wait_us
      - frame-slot render-fence recycle wait

  - graphics_wait_us
      - graphics bridge fence wait

  - acquire_us
  - present_us
  - presenter_completion_us
      - 当 completion source 为 image reacquired 时记录 acquire 时间

  兼容字段 wait_us 仍输出，值等于 graphics_wait_us，便于旧工具读取。

  新 reason 候选：

  - raster_workload
  - gpu_raster_workload
  - cpu_producer
  - frame_slot_wait
  - graphics_fence_wait
  - presenter_completion_wait
  - present_or_acquire
  - external_scheduler

  ### 3. GPU 分位数

  RB0-STATS 新增：

  - GPU Raster median/P95/P99
  - GPU Draw median/P95/P99
  - GPU bridge import+export median/P95/P99
  - frame-slot wait median/P95/P99
  - presenter completion median/P95/P99
  - unattributed median/P95/P99

  采样脚本已经更新解析这些字段，并写入每轮 manifest。

  ### 4. CPU/GPU frame 对应

  最终实现不再将旧 slot GPU timestamp 直接附给当前 CPU frame。

  流程：

  - GPU timing 带有自身 mixed_gpu_timing_frame。
  - 低扰动采样按 frame ID 查找对应 CPU 样本。
  - GPU Raster/Draw/bridge 数据回填到对应 frame。
  - 慢帧输出中的：

  frame=<N> gpu_timing_frame=<N>

  应相等。

  - 退出前尚未回收的最后两个 GPU timestamp 不进入 GPU 分位数。
  - reason 只使用已经映射到同一 CPU frame 的 GPU Raster。

  ### 5. 未覆盖时间

  输出：

  unattributed_us

  定义为 whole-loop 减去最大单项候选。

  必须明确：

  - 它不是互斥时间分解。
  - 不能与其他字段相加。
  - CPU wall 与 GPU timestamp 可能重叠。
  - 它只用于发现当前统计仍未覆盖的大块时间。

  ## 七、新统计格式门禁

  最终有效格式/同帧映射检查目录：

  tmp/gpu-rb0-frame-map-check-20260922-2

  命令：

  powershell -ExecutionPolicy Bypass -File tools/gpu_rb0_sampling.ps1 `
    -Rounds 1 -NoAudit `
    -OutputDirectory tmp/gpu-rb0-frame-map-check-20260922-2

  结果：

  - 四场景全部通过。
  - RB0-STATS 新格式通过脚本解析。
  - RB0-SLOW 新字段存在。
  - frame 与 gpu_timing_frame 在有有效 timing 的慢帧中一致。
  - 无残留进程。

  一次较早的格式目录：

  tmp/gpu-rb0-frame-map-check-20260922

  运行时 package 尚未刷新，使用的是旧 exe，不应用于验证最终同帧映射。

  另一个中间格式目录：

  tmp/gpu-rb0-wait-format-check-20260922

  证明新 wait 字段初版可运行，但 GPU timestamp 仍只是打印 source frame，没有最终回填。最终结论以 gpu-rb0-frame-map-check-20260922-2 为准。

  最终格式检查中的 near 60 示例显示：

  - frame-slot wait：约数微秒
  - presenter completion/acquire：约数微秒
  - graphics fence wait：可达数十毫秒
  - GPU Raster：约数十毫秒
  - 未覆盖时间：约 60–90 ms

  但这只有一轮，不能作为正式性能结论。

  ## 八、验证结果

  ### Windows build

  通过：

  .\windows\NativeCodex.ps1 build

  ### Logic test

  通过：

  .\windows\NativeCodex.ps1 test

  ### GPU Quick

  通过：

  tmp/gpu-acceptance-20260922-103554

  命令：

  powershell -ExecutionPolicy Bypass -File tools/gpu_acceptance.ps1 -Quick

  ### GPU Full

  通过：

  tmp/gpu-acceptance-20260922-104218

  命令：

  powershell -ExecutionPolicy Bypass -File tools/gpu_acceptance.ps1 -Full

  Full 已覆盖并通过：

  - logic
  - hosted graphics
  - resource cache
  - raster differential
  - mixed executor
  - mixed native resize
  - near native
  - character vertex diff
  - near/mid CPU/GPU
  - thin-far
  - presenter-300
  - world-cycle
  - Campaign 320
  - near 30/60
  - character rollback
  - 固定 capture

  没有发现画面、提交、资源生命周期或 presenter 合同回归。

  ## 九、文档已更新

  本轮已经更新：

  rasterfall/docs/gpu-raster-bridge-plan.md
  rasterfall/docs/gpu-current-state.md
  rasterfall/docs/gpu-rendering-architecture.md

  内容包括：

  - 平衡方案正式五轮结果。
  - near 60 双态仍存在。
  - 高性能切换平衡的中途采样无效。
  - 电源方案不是双态的充分原因。
  - wait 分类细分。
  - GPU timestamp 按 frame ID 回填。
  - GPU 分位数和未覆盖时间。
  - RB-0 仍不能退出。

  顶部文档日期仍为 2026-09-22，源码核对基线仍适用。

  ## 十、新会话的第一优先任务

  当前最重要的缺口是：

  > 新统计实现之后，尚未在平衡方案下完成正式五轮低扰动采样。

  开始前执行：

  git status --short
  Get-Process -Name rasterfall -ErrorAction SilentlyContinue
  powercfg /getactivescheme

  必须确认：

  - 工作区修改完整。
  - 没有 rasterfall.exe。
  - 电源方案为“平衡”。
  - 不启动其他 GPU 程序。
  - 不使用高性能方案。

  然后运行：

  powershell -ExecutionPolicy Bypass -File tools/gpu_rb0_sampling.ps1 `
    -Rounds 5 -NoAudit

  该组必须使用当前新 package，即已经包含：

  - frame-slot wait
  - graphics fence wait
  - presenter completion
  - GPU frame ID 回填
  - GPU Raster/Draw/bridge 分位数
  - unattributed time

  ## 十一、新五轮需要重点分析什么

  ### 1. near 60 双态是否再次出现

  记录每轮：

  - whole-loop median/P95/P99
  - GPU Raster median/P95/P99
  - GPU Draw median/P95/P99
  - GPU bridge median/P95/P99
  - slot wait median/P95/P99
  - graphics fence wait P95
  - presenter completion P95
  - unattributed median/P95/P99

  不使用 maximum 或启动首帧做判断。

  ### 2. 慢帧 reason 分布

  重点统计：

  - gpu_raster_workload
  - frame_slot_wait
  - graphics_fence_wait
  - presenter_completion_wait
  - external_scheduler

  比较 near 0、30、60 和 Campaign。

  ### 3. Frame-slot wait 是否可以排除

  最终格式检查中 slot wait 约数微秒。

  如果正式五轮仍保持微秒级：

  - 可以排除双帧 slot recycle 本身是 near 60 长尾主因。
  - 旧 slot_or_fence_wait 应重新解释为主要来自 graphics fence，而不是 frame-slot。

  ### 4. Presenter 是否可以排除

  如果：

  - acquire 仍是微秒级
  - present 仍是微秒级
  - presenter completion 仍是微秒级

  则 presenter API 与 image reacquire 不是双态主因。

  ### 5. Graphics fence 与 GPU Raster 是否同步

  这是最关键的问题。

  需要按同帧比较：

  - graphics fence wait
  - GPU Raster
  - GPU bridge
  - whole-loop
  - unattributed

  可能的结果：

  #### A. GPU Raster 和 graphics fence 同步增长

  说明 graphics fence 很可能只是观察到前序 GPU Raster completion，而不是 graphics Draw 本身慢。

  下一步应继续 RB-0，按 fence 调用点拆分：

  - Raster → Draw import
  - Draw batch completion
  - Draw → Raster export
  - final Raster/present 前

  #### B. GPU Raster稳定，但 graphics fence 大幅增长

  需要调查：

  - queue 上未被 timestamp 覆盖的工作
  - bridge同步
  - driver/系统调度
  - timestamp覆盖缺口
  - CPU 等待位置

  #### C. graphics fence稳定，未覆盖时间增长

  继续补顶层非嵌套 phase：

  - update
  - producer
  - freeze/preflight/pack
  - GPU submit/wait
  - presenter
  - outer-loop remainder

  不要直接进入 RB-1。

  ### 6. Bridge 必须继续核对

  确认：

  - near 0/30/60 仍是 12 次、88,473,600 bytes。
  - Campaign 仍是确定的 14/16 次状态。
  - workload 与 bridge 没有随双态漂移。

  ## 十二、新五轮之后可能允许的 RB-0 修改

  只有新五轮仍不足以解释双态时，允许继续：

  1. 按 graphics fence 调用点拆分 wait：
      - import
      - draw batch
      - export
      - final

  2. 为每个 wait 输出：
      - frame ID
      - span/layer
      - 前后 producer
      - wall wait
      - 对应 GPU timestamp

  3. 补少量顶层非嵌套 phase。
  4. 补 Vulkan adapter 身份：
      - driver version
      - vendor ID
      - device ID
      - Vulkan API/driver信息

  5. 改进脚本跨轮摘要，直接汇总新增 GPU/wait 字段。

  仍禁止：

  - 改提交顺序
  - 合并 span
  - 跳过 bridge
  - 扩展 Graphics 类型
  - 改变画面
  - producer迁移
  - 开始 RB-1

  如继续修改统计代码，至少运行：

  .\windows\NativeCodex.ps1 build
  .\windows\NativeCodex.ps1 test
  powershell -ExecutionPolicy Bypass -File tools/gpu_acceptance.ps1 -Quick

  若触及 GPU/presenter/wait/timestamp读取合同，再补：

  powershell -ExecutionPolicy Bypass -File tools/gpu_acceptance.ps1 -Full

  ## 十三、仍未完成的 RB-0 退出条件

  RB-0 当前不能退出，原因包括：

  - 新统计尚未完成平衡方案正式五轮。
  - near 60 双态尚未最终解释。
  - graphics fence 到底在等待哪一段 GPU 工作尚未拆清。
  - 未覆盖时间仍可能较大。
  - Intel driver version 未覆盖。
  - RTX 3050/另一物理 GPU 同 package 五轮尚未完成。
  - presenter-300 跨设备复核未完成。

  RTX 3050 继续暂缓，除非用户之后明确恢复。

  ## 十四、最终检查要求

  任何新一轮结束后执行：

  Get-Process -Name rasterfall -ErrorAction SilentlyContinue
  git diff --check
  git status --short
  powercfg /getactivescheme

  报告：

  - 是否有残留进程
  - 活动电源方案
  - 新证据目录
  - package commit/exe身份
  - 运行数量和退出码
  - 新 reason 分布
  - bridge稳定性
  - 未覆盖项
  - 是否仍停留在 RB-0

  不要创建提交，除非用户明确要求。
