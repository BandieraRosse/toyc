# Toyc 考古文档汇总

本文件由 `tools/package_archaeology.sh` 根据 `docs/archaeology/` 下的 Markdown 文件生成。每个章节标题标注原始文件路径。

---

## 文件：`docs/archaeology/README.md`

# Toyc 考古计划

本文档集研究 Toyc 的项目谱系、技术演化和开发实践。当前先建立可扩展框架，后续按主题逐步
补充提交级证据、代码对照、开发者回忆和外部材料。考古范围以 Tinylibc、ToyCCompiler 与
Toyc 为主体，同时追溯 Tinylibc 从 SC7 团队操作系统项目分离的前史。

## 叙事主轴

作者在考古计划启动时将这条主线概括为：

> 这条 C 语言项目的演进，是我逐渐深入理解计算机底层世界，并且提高 C 语言项目能力
> （已经不是 C 编程能力了），并且逐渐使用 coding agent 的过程。

这段口述是后续组织材料的核心线索，而不是已经由 Git 单独证明的结论。文档将沿三条相互关联
但不能互相替代的轴展开：

- **底层理解**：从操作系统内核、用户态接口和 libc，进入编译器、自举工具链及更完整的运行时。
- **项目能力**：从写 C 代码，发展到处理架构边界、构建系统、测试体系、跨平台和大型项目演进。
- **协作工具**：从个人调试与网页对话辅助，逐渐转向 Claude Code、DeepSeek API 等 coding agent
  参与的开发方式。

## 核心问题

1. Tinylibc 如何从手写 libc 与用户程序，发展出最初的 C 编译器实验？
2. 为什么编译器从 Tinylibc 中独立为 ToyCCompiler，它怎样完成自举工具链？
3. Toyc 如何继承 ToyCCompiler 的 Git 历史，并重新吸收 Tinylibc 的库与应用？
4. 网页对话、Claude Code、DeepSeek API 等工具分别怎样影响了三个时期的工作方式？
5. 哪些设计是连续演化，哪些是迁移、重写、改名或事后重新解释？

## 已知主线

```text
SC7（2025 年三人团队操作系统竞赛项目）
    │  基于 MIT XV6，支持 RISC-V 与 LoongArch
    │  2025-10-17 Tinylibc 根提交说明从 SC7 分离
    ▼
Tinylibc（项目与代码源头）
    │  2026-06-30 起在仓库内发展 tcc 编译器
    │  2026-07-04 提取为独立仓库；未保留 Tinylibc 的 Git 祖先链
    ▼
ToyCCompiler（独立、自举工具链时期）
    │  2026-07-11 原仓库以 README 重定向宣告迁往 toyc
    │  toyc 直接延续其完整提交链
    ▼
Toyc（“two parents”整合时期）
    │  2026-07-23 至 24 日重新引入 Tinylibc 的库与应用树
    ▼
当前仓库（工具链、Tinylibc、应用与 Rasterfall 并存）
```

这里必须区分两种“祖先”：

- **Git 祖先**：Toyc 与 ToyCCompiler 共享根提交 `22ffcc8`；Toyc 的历史直接延续 ToyCCompiler。
- **项目与代码祖先**：该根提交明确写明编译器从 Tinylibc 提取；Tinylibc 更早的提交没有被接到
  Toyc 的 Git DAG 中，因此需要跨仓库考证。

## 时期入口

- [SC7 前史](periods/sc7.md)：团队操作系统竞赛、内核经验与 Tinylibc 的分离背景。
- [Tinylibc 时期](periods/tinylibc.md)：手写阶段、libc/应用生态、仓库内编译器的诞生。
- [ToyCCompiler 时期](periods/toy-c-compiler.md)：独立仓库、自举、汇编器与链接器闭环。
- [Toyc 时期](periods/toyc.md)：双重来源、命名统一、Tinylibc 回归及后续扩展。
- [初步时间线](timeline.md)：跨仓库的关键边界提交。
- [证据与方法](sources.md)：资料优先级、引用格式、待核问题和复查命令。
- [关键问题台账](questions.md)：跨会话逐题回答、核查和写入专题文档的工作入口。

## 当前结论的范围

本轮只建立框架并确认仓库级传承。关于 AI 参与强度和个人开发方式，当前依据主要是作者口述；
提交中的 `Co-Authored-By` 可证明部分提交显式记录了 Claude，但不能单独量化 AI 贡献，也不能
证明未署名提交没有使用 AI。DeepSeek API 和网页对话的具体使用过程仍需聊天记录、脚本、账单、
本地日志或作者访谈等材料交叉验证。

## 下一阶段

- 为 Tinylibc 建立阶段划分，先覆盖编译器出现前的手写主线。
- 对照 SC7 的 `tlibc` 分支与 Tinylibc 根提交，确认分离时继承的代码和设计。
- 对照 Tinylibc `87d61e0` 附近源码与 ToyCCompiler 根提交，形成文件级来源表。
- 按提交重建 ToyCCompiler 从最小编译器到 stage-10 收敛的过程。
- 找出 Toyc 引入 Tinylibc 时的准确源快照，而不只依赖提交说明。
- 建立“作者回忆待访谈”清单，记录工具使用、关键决策、失败尝试与情绪背景。

---

## 文件：`docs/archaeology/periods/sc7.md`

# SC7 前史

## 研究范围

SC7 是作者在 2025 年参加操作系统内核比赛时参与的三人团队作品。它不是 Tinylibc、
ToyCCompiler、Toyc 这条个人项目主线的简单第一版，但构成了理解其底层知识、用户态设施和
C 项目经验来源的重要前史。

## 赛事时间背景

外部赛程资料显示，2025 年内核实现赛道初赛作品提交截止为 2025-06-30；决赛相关测试和现场
环节集中到 2025-08-20 左右。不同通知对决赛阶段的切分略有差异：一份赛程将决赛阶段列为
2025-07-12 至 2025-08-12、再进入 8 月 13 日起的现场阶段；比赛平台则列出 2025-07-23 至
2025-08-20 的决赛阶段 1，并将 QEMU/开发板测试安排在 8 月 20 日。这里仅用来辅助划分提交
阶段，不把平台阶段直接等同于队内分工。

## 已确认事实

- 仓库远端为 `WHU-SC7/SC7`，主分支本轮观察到 620 个提交。
- 根提交 `ec37618` 日期为 2025-03-23，说明首次版本支持 RISC-V 与 LoongArch 双架构 UART 输出。
- README 将 SC7（martCore7）描述为基于 MIT XV6、使用 C 开发、支持 RISC-V 与 LoongArch 的
  教学操作系统，并列出三名学生成员和指导老师。
- 仓库按初赛、决赛一阶段和决赛现场赛保存分支及文档。
- 功能涉及进程与线程、物理和虚拟内存、VFS/ext4、系统调用、信号、procfs、多核启动与用户程序。
- 决赛现场赛文档记录了动态链接、符号链接、文件位置同步、物理开发板适配，以及通过拆分提交
  降低复杂度的经验。
- 仓库存在远端 `tlibc` 分支；其与 Tinylibc 根提交的准确关系尚未核对。

## 作者提交记录线索

Toyc 与 SC7 中的多个历史提交身份已由作者确认属于同一作者，本文不保存具体姓名、邮箱或账号标识。
这些身份记录可以归并分析；提交作者仍只证明提交归属，不等于该提交中的每一部分都由作者独立完成。

按提交说明和变更路径，作者可确认或高度可信地负责以下工作线：

- **双架构底层与启动链路**：早期主导 RISC-V 用户态进入、trap/异常返回和系统调用框架，并参与
  LoongArch 方向的早期框架工作，代表提交为 `19e560d`、`ecf7de8`、`fb5ef68`、`564105d`、
  `e21c43e`。但 LoongArch 虚拟化用户态最终由队友推进，不能写成作者独立解决。
- **设备、平台和多核尝试**：主导 RISC-V SBI、VirtIO 磁盘、LoongArch PCI/VirtIO、时钟中断及
  多个板端适配。多核部分确实写过启动、调度和服务进程代码，但最终没有成功，不能表述为“完成
  多核适配”；相关提交包括 `df5eeef`、`1ae3eba`、`d31e2c6`、`9df96f0`、`8f3ca98`、`a960728`。
- **文件系统接口与文件相关系统调用**：实现或修复 `getcwd`、`mkdirat`、`chdir`、`getdents64`、
  `unlink`、`link`、`symlink`、`lseek`、`pwrite64`、`prlimit64` 等，并处理 ext4 上层接口、路径
  和错误码，代表提交为 `e86f184`、`e23f00a`、`8436f13`、`f0d02af`、`57567e9`、`4c92f0f`。
  但初始 ext4/lwext4 移植由队友完成，作者当时也尚未真正理解 ext4；后续 VFS/ext4 的重构和
  修复还包括队友提交，不能统一归为作者成果。
- **真实硬件适配与测试**：推进 VisionFive 和 2K1000 的启动、文件系统接入、虚拟内存和板端调试，
  例如 `974c487`、`460709e`、`7d99e47`、`5bbdf4d`、`d389233`、`df580fe`。其中 VisionFive
  板载 SD 存储驱动的主要移植提交为作者的 `74aee85`/`df580fe`，但作者确认该部分得到队友帮助，
  提交记录本身无法量化协助范围。
- **LoongArch 虚拟化协作**：作者提交过 LoongArch 的 PCI/VirtIO 磁盘路径（`407f2fe`、`9df96f0`）
  和相关调试，但作者确认没有解决 LoongArch 虚拟化问题；用户态虚拟内存、陷阱和从磁盘执行 ELF
  的关键推进来自队友。因此这里应记录为队友主导、作者参与协作，而非作者独立负责。
- **Tinylibc 用户态生态**：在 `origin/tlibc` 上完成库构建整理、Shell、`ls`/`touch`/`cat`/`rm`/
  `echo`/`pwd`/`cd`/`mkdir`/`rmdir`/`cp`/`mv`/`help` 等命令，以及对应的 `read`、`openat`、
  `fstat`、`fork`、`wait`、目录和删除操作；代表提交为 `5aa1cb2` 至 `d2363da`。

SC7 还有两条主要队友提交线，合并提交也使用过团队账号。作者回忆：前期基础系统调用约一半由
作者完成，后续新增系统调用中作者约完成三分之一；最关键的虚拟内存和文件系统由队友承担很多，
后期动态链接问题也主要由队友推进。作者后期重心转向设备适配，并分担一部分系统调用功能。
这些比例是作者回忆中的粗略估计，分母和阶段边界仍需进一步定义。

作者还回忆，约 2025 年 6 月中旬曾独立处理过一次 glibc 动态链接启动问题：程序访问一个启动
时尚未通过 `mmap` 映射的异常地址，作者排查后采用直接映射该地址但不写入内容的办法，使程序
能够启动。对应提交线索为 `64d9400`，但后续 `mmap`、虚拟内存和动态链接工作已交由队友推进，
作者在决赛阶段也未继续负责这些部分；具体代码演化仍需逐提交复原。

## 按提交顺序推断的分工

以下分段依据提交密度、变更主题和作者回忆，不直接等同于比赛官方阶段：

- **早期原型与基础框架（2025-03 至 2025-04 上旬）**：作者强烈主张用 C 开始项目，理由是希望
  自己掌控系统实现，并快速给出原型。提交记录显示作者主要推进双架构启动整理、RISC-V 用户态、
  trap 返回、基础系统调用、进程入口和部分设备路径；LoongArch 的虚拟化用户态尚未由作者解决。
- **系统能力扩展与协作整合（2025-04 中旬至 2025-06）**：作者继续推进 RISC-V SBI/VirtIO、
  LoongArch PCI/VirtIO、时钟和用户态接口，并分担一部分文件相关系统调用。初始 ext4/lwext4 由
  队友移植，虚拟内存和 LoongArch 用户态虚拟化由队友推进，动态链接相关问题也主要由队友解决。
- **后期适配与 Tinylibc 分支（2025-07 至 2025-10）**：作者重心转向 VisionFive/2K1000、SD
  存储和板端适配，同时继续完成部分系统调用，并在 `origin/tlibc` 上发展 Shell 和 Tinylibc。
  多核代码在这一阶段持续出现，但最终没有成功运行，不能计为已完成能力。

还需要补齐的不是“谁提交了代码”，而是：比赛官方阶段与上述时间段的对应关系、作者所说基础系统调用
比例的统计口径、板载存储驱动中队友帮助的具体部分，以及动态链接问题的关键决策和最终修复归属。

## Git 记录核对结论

### C 原型与早期基础

- `ec37618`（2025-03-23）是双架构 UART 原型；`cae8d60`、`bb7d03b`、`8e59241` 继续推进
  Makefile/硬件抽象、printf 和进程模块。结合作者回忆，可以确认作者在项目最初推动 C 路线并快速
  形成原型，早期主线重点是启动、进程和用户态基础，而不是 ext4。
- `19e560d`、`ecf7de8`、`fb5ef68`、`564105d`、`e21c43e` 显示作者连续推进 RISC-V 用户态、
  trap 返回和基础系统调用，并开始处理 LoongArch 用户态框架。这些提交支持“作者主导早期框架”，
  但不支持“作者独立完成双架构虚拟化”。

### 虚拟内存、文件系统与动态链接的分工

- `02649e8` 引入 lwext4、VFS_EXT4 和 VFS；`c69bb28`、`3b3688e` 继续重构 ext4/VFS。它们
  属于队友的核心文件系统工作，作者在该阶段并未掌握 ext4 内部实现。
- 作者的 `e86f184`、`e23f00a`、`03855b4`、`f0d02af`、`57567e9`、`3df36c0`、`4c92f0f` 等
  提交，主要集中在文件系统之上的路径处理、系统调用、错误检查和测试适配；应表述为“使用并扩展
  队友提供的文件系统基础”，而非“负责 ext4”。
- `61bebb2`、`217dade`、`5ecb529`、`127fca0`、`2fa9e37`、`6cd42ce`、`8f721c0` 等提交
  显示队友持续推进 LoongArch/通用虚拟内存、lazy load、VMA 和动态链接；`b3a44a8`、`a04b0d0`
  进一步完成 LoongArch 从磁盘读取并执行 ELF 的关键路径。这支持“LoongArch 虚拟化由队友成功
  推进”的回顾结论。
- `64d9400`（2025-06-17）是作者在 glibc 动态链接早期针对异常地址 `0x000000010000036e`
  的直接映射 workaround；紧接着 `8a698fc` 仍记录 LoongArch glibc 动态链接问题，`5ecb529`
  （2025-06-18）记录队友通过增加映射长度等方式解决。后续动态链接、mmap 和虚拟内存不应归为
  作者负责。

### 设备适配与多核

- `d31e2c6`、`407f2fe`、`9df96f0` 显示作者搭建并验证 LoongArch PCI/VirtIO 磁盘设备路径，
  但这不等于作者解决了 LoongArch 用户态虚拟化；后者由队友的 `b3a44a8` 等提交完成关键推进。
- `74aee85`（2025-07-31）和 `df580fe`（2025-08-20）新增完整的 VisionFive SD 驱动 C 文件，
  包括 `sdio_cmd.c`、`sdio_ops.c`、`sdio_utils.c` 和 `vf2_sd_timer.c`，提交说明记录虚拟内存下
  读写测试通过。Git 能确认作者提交并接入了可用驱动，不能从提交对象判断队友协助的具体范围。
- `a960728`、`8f3ca98`、`9a3465e`、`db40a0b` 等提交确实包含多核启动、调度和服务进程代码，
  部分提交说明使用“运行成功”字样；但作者回顾最终多核没有成功，故历史文档应同时保留“当时提交
  的成功宣称”和“最终能力未稳定”的差异，不能把它列为已完成模块。

## 与后续主线的可能联系

以下只是研究方向，不能在完成代码对照前写成确定的继承关系：

- 系统调用封装、用户态入口、C 库与 shell 经验可能成为 Tinylibc 的直接技术背景。
- 双架构内核、物理板和 QEMU 调试可能影响了后来对 ABI、ELF、freestanding 运行时的理解。
- 决赛文档已经表现出“拆分复杂问题、用连续提交逐步恢复能力”的工程方法，值得与后续编译器
  密集修复过程对照。

## 待写章节

- 比赛背景、阶段和团队目标
- 三名成员的分工及作者具体负责范围
- 从 XV6 与参考项目继承的基础
- 作者负责的内核、用户态和调试工作
- `tlibc` 分支的形成与用途
- 比赛结束后继续个人开发的动机
- SC7 到 Tinylibc 的代码、知识与工程方法迁移

## 边界原则

SC7 是团队成果。后续文档引用其代码与成果时，必须区分团队整体成果、外部参考项目、成员分工
和作者个人完成的部分；不能因为 Tinylibc 由作者延续，就把 SC7 的全部工作归入个人项目史。

---

## 文件：`docs/archaeology/periods/tinylibc.md`

# Tinylibc 时期

## 研究范围

从 SC7 的用户态与 libc 实验、Tinylibc 根提交开始，研究 libc、系统调用封装、用户程序、自托管
构建工具，以及 2026-06-30 起仓库内 tcc 的快速形成。重点区分团队项目遗产、个人手写代码、
参考原型、网页对话辅助和后期 Claude 署名提交。

## 已确认锚点

- `fce216c`：Tinylibc 仓库根提交，说明项目从 SC7 分离并转向 x86_64。
- `4670d9f`：加入最小编译器 Phase 1，提交元数据含 Claude 共同作者。
- `d996c17`：提交说明记录 9/9 自编译通过。
- `9921705`：提交说明记录 `tcc.c` 自编译通过。
- `2213ae5`：`tmake -T` 引入以 tcc/tas 替代 gcc 的路径。
- `87d61e0`：ToyCCompiler 独立前夕的重要编译器修复锚点。

## 待写章节

- SC7 前史与 Tinylibc 的分离动机
- SC7 `tlibc` 分支与 Tinylibc 根提交的文件级关系
- freestanding libc 的最早结构
- 应用驱动的库扩展方式
- `tmake` 与自包含愿景
- 最初编译器的五阶段推进
- 手写、参考原型与对话辅助的边界
- 失败分支、合并提交与未进入主线的实验

## 待核问题

- “手写时期”的准确时间范围和判断标准是什么？
- 复制过哪些代码原型，来自何处，后来保留、改写或移除了哪些部分？
- 2026-06-30 前后开发工具发生了什么变化？
- Tinylibc 仓库中的 compiler 分支与主线合并过程如何还原？

## 作者回忆补充

作者回忆，最初主张改用 C 开始项目，核心理由是希望掌控系统实现；相比沿用学长的 C++ 方案，
C++ 用于系统开发更重。作者随后快速给出了一个 C 原型。这是动机和过程的作者口述，仍需用早期
提交、原型文件和开发记录补足细节。

---

## 文件：`docs/archaeology/periods/toy-c-compiler.md`

# ToyCCompiler 时期

## 研究范围

从 2026-07-04 的独立根提交到 2026-07-11 的仓库重定向，研究编译器如何脱离 Tinylibc 的应用
语境，集中完成自身编译、汇编、链接和多阶段收敛。另设开发工具线索，考察 Claude Code 与
DeepSeek API 的实际分工，而不从提交署名直接推断贡献比例。

## 已确认锚点

- `22ffcc8`：从 Tinylibc 提取 tcc，创建独立 Git 历史；也是当前 Toyc 的根提交。
- `0252321`：加入自举自托管测试脚本。
- `961fbf6`：提交说明记录自举到 stage 10。
- `8b2ea1f`：自举成功宣言。
- `687ed29`：提交说明称该次 README 修改“没有 AI”，是研究署名与工具使用认知的重要材料。
- `7adcf8f`：种子、gcc-free 构建与 tas 自举。
- `c951c79`、`b3c5145`、`9948ea0`：tld 出现并完成链接器自举闭环。
- `58ac389`：原仓库以 README 重定向形式退役。

## 待写章节

- 从 Tinylibc 提取时的代码快照
- 自编译阻塞问题与密集修复节奏
- 测试套件如何塑造编译器能力
- stage 2 到 stage 10 的收敛含义
- toyas 与 toyld 如何替代 GNU 工具
- 种子二进制的建立、退役和重新引入
- Claude Code 工作流、DeepSeek API 工作流及人工调试
- README 宣言、修订与项目身份变化

## 待核问题

- 独立仓库为何选择新根提交，而不是保留 Tinylibc 历史？
- 哪些任务主要由 Claude Code 承担，哪些由 DeepSeek API 承担？
- “高强度使用”可由哪些会话、日志和提交模式量化？
- 自举成功的每个阶段在当时实际使用了哪些外部工具？


---

## 文件：`docs/archaeology/periods/toyc.md`

# Toyc 时期

## 研究范围

从 2026-07-11 的 “new beginning, two parents” 开始，研究 Toyc 如何在 ToyCCompiler 的 Git
主线上重新整合 Tinylibc，并扩展工具链、库、应用和后来的 Rasterfall。该时期跨度较大，后续
可能拆成“整合期”“工具链扩展期”“Rasterfall 主线期”。

## 已确认锚点

- `643287b`：首次以 Toyc 名义描述来自 ToyCCompiler 与 Tinylibc 的双重来源。
- `a26e7a6`：将 `app/` 改为 `compiler/`，为后续应用目录腾出边界。
- `5bffca4`：工具链统一采用 `toy*` 命名，避免与 TinyCC 混淆。
- `af6bc30`：引入 Tinylibc 的完整库、应用、架构头和 GCC 构建规则。
- `2698c81`、`75a10cd`：2026-09-03 前后，仓库协作说明和文档主线转向 Rasterfall。

## 待写章节

- “two parents”的项目定位
- Tinylibc 回归时的文件来源和差异
- tcc/tas/tld 到 toyc/toyas/toyld 的命名统一
- `toyar`、浮点、库兼容与应用生态扩展
- Toyc 编译器维护与 Tinylibc 上游的后续关系
- Rasterfall 的诞生及仓库重心迁移
- 从编译器实验到长期工程化的工作流变化

## 待核问题

- `af6bc30` 引入的 Tinylibc 对应哪个上游提交或工作树状态？
- 2026-07-23 Tinylibc 自身又引入 ToyCCompiler 时，两仓是否形成双向移植？
- 何时可以认为项目名称、二进制名称和仓库身份都完成了 Toyc 化？
- Rasterfall 与工具链之间是能力验证、应用生态，还是逐渐独立的新项目主线？


---

## 文件：`docs/archaeology/questions.md`

# 关键问题台账

本文档用于跨会话逐项推进考古工作。每个问题先记录作者回答，再寻找 Git、代码、同期文档和
外部材料交叉验证，最后将稳定结论写入对应时期或专题文档。不要因问题已得到口述回答就删除；
应更新状态并链接最终落点。

状态约定：`待回答`、`已有概述`、`待核查`、`已写入`。

## Q00：整条主线意味着什么？

**状态：已有概述**

作者总述：这条 C 语言项目的演进，是逐渐深入理解计算机底层世界、提高 C 语言项目能力
（已不只是 C 编程能力），并逐渐使用 coding agent 的过程。

后续追问：

- “C 编程能力”和“C 项目能力”的分界具体体现在哪些事件？
- 哪些阶段分别代表底层理解、项目能力和 agent 使用方式的跃迁？
- 这三条变化是互相促进，还是也曾发生冲突？

落点：`README.md` 的叙事主轴，以及未来的总结专题。

## Q01：SC7 中作者实际负责什么？

**状态：待核查**

作者已确认，Toyc 与 SC7 中的多个历史提交身份均属于本人。为保护隐私，考古文档不记录具体姓名、
邮箱或账号标识。

提交记录与作者回忆显示，作者主导了 RISC-V 用户态/异常/系统调用早期框架、设备适配、部分文件
系统调用、后期 Tinylibc 用户态库、Shell 和文件操作命令。初始 ext4/lwext4 移植由队友完成（`02649e8`）；
LoongArch 虚拟化由队友成功推进（`5ecb529`、`b3a44a8`、`a04b0d0`）；多核虽有代码提交但最终未
成功（`a960728`、`8f3ca98`、`9a3465e`、`db40a0b`）；VisionFive 板载 SD 驱动由作者提交可用
实现（`74aee85`、`df580fe`），但有队友协助。详见 `periods/sc7.md` 的“Git 记录核对结论”。

- 初赛、决赛一阶段和现场赛分别承担哪些模块？
- 哪些工作是独立完成，哪些是与队友共同完成？
- 哪些 bug 或现场问题最影响后来开发方式？
- 比赛结束后为什么继续发展用户态和 libc？

当前已确认：作者身份归并、主要工作方向及三类协作边界。Git 记录已给出按阶段的主要工作线，但仍需确定：基础
系统调用“约一半/约三分之一”的统计口径、LoongArch 虚拟化中队友提供的代码/调试边界、SD 驱动
中队友帮助的具体内容、ext4 接口层由谁负责哪些后续修改，以及 `64d9400` 前后动态链接修复的
完整代码演化。

落点：`periods/sc7.md`。

## Q02：Tinylibc 怎样从 SC7 分离？

**状态：待核查**

- SC7 的 `tlibc` 分支是什么，何时创建，由谁主要开发？
- Tinylibc 根提交来自哪个 SC7 提交、分支或工作树？
- 分离时复制了哪些目录，放弃了哪些内核依赖？
- 目标从“为 SC7 提供用户态”变成了什么？

落点：`periods/sc7.md`、`periods/tinylibc.md` 和 `timeline.md`。

## Q03：Tinylibc 的手写时期如何工作？

**状态：已有概述**

已知口述：该时期主要手写，通过网页端对话学习和复制部分代码原型，调试与主要开发由作者进行。

后续追问：

- 大致起止时间和阶段划分是什么？
- 使用哪些网页服务或模型，主要用于学习什么？
- 复制过哪些代码原型，来源和后续改写情况如何？
- 哪些模块最能代表作者独立设计与调试？
- 什么时候开始从单个功能开发转向整体构建、测试和项目组织？

落点：`periods/tinylibc.md`，未来可增设开发方法专题。

## Q04：为什么开始写 C 编译器？

**状态：待回答**

- 直接动机是编译 Tinylibc、自举、学习编译原理，还是其他目标？
- 第一个编译器阶段开始前做过哪些准备或失败实验？
- 当时如何选择 lexer、parser、代码生成和 ELF writer 的结构？
- 最初怎样定义“成功”？

作者已补充，项目最初选择 C 的直接动机是追求对系统实现的掌控，认为沿用学长的 C++ 方案偏重，
并在此基础上快速写出原型。这里仍需确定原型的具体提交、功能边界，以及从系统 C 原型转向 C 编译器
的时间点和直接触发事件。

落点：`periods/tinylibc.md`。

## Q05：为什么建立独立的 ToyCCompiler 仓库？

**状态：待回答**

- 为什么选择新根提交而不是保留 Tinylibc Git 历史？
- `22ffcc8` 对应 Tinylibc 的哪个源码状态？
- 独立是为了隔离目标、提高 agent 效率，还是准备公开展示？
- 当时是否已经计划之后建立 Toyc？

落点：`periods/toy-c-compiler.md`。

## Q06：Claude Code 与 DeepSeek API 如何分工？

**状态：已有概述**

已知口述：ToyCCompiler 时期高强度使用 Claude Code 和 DeepSeek API。

后续追问：

- 两者分别负责设计、实现、调试、测试、审查或文档中的哪些部分？
- 是否存在稳定的多模型工作流？作者怎样验证和取舍输出？
- 哪些关键突破主要来自作者判断，哪些明显受 agent 推动？
- `Co-Authored-By` 的添加规则是什么？未署名提交是否也可能使用 AI？
- `687ed29` 所说“这一次提交没有 AI”具体表达什么？
- 还保留哪些会话、脚本、日志或账单，公开边界是什么？

落点：`periods/toy-c-compiler.md`，未来可增设 agent 协作专题。

## Q07：“自举成功”包含哪些阶段？

**状态：待回答**

- 编译单个自身源文件、构建可运行自身、多 TU 自编译分别何时完成？
- toyas 与 toyld 分别在何时消除 GNU `as` 和 `ld` 依赖？
- 种子二进制怎样生成、验证、退役和恢复？
- stage 2–10 字节级收敛在当时具体比较哪些文件？
- “零外部依赖”采用什么边界定义？

落点：`periods/toy-c-compiler.md` 和未来的自举专题。

## Q08：Toyc 为什么重新整合 Tinylibc？

**状态：待回答**

- “two parents”是在什么时候形成的项目定位？
- 为什么不继续维持两个独立仓库？
- `af6bc30` 引入的 Tinylibc 对应哪个快照？
- Tinylibc 同期以 `32436f1` 引入 ToyCCompiler，为何发生双向移植？
- 改名 Toyc 除避免 TinyCC 混淆外，是否代表新的项目身份？

落点：`periods/toyc.md`。

## Q09：何时从 C 编程转向 C 项目能力？

**状态：待回答**

- 哪些经历让关注点从函数实现转向模块边界、构建、测试和维护？
- 自举、跨架构、跨平台、网络或大型重构分别带来什么能力变化？
- 是否存在“代码能跑，但项目不可维护”的明确转折事件？
- coding agent 是否促使项目规模扩大，也是否制造过新的复杂度？

落点：跨时期总结专题。

## Q10：Rasterfall 属于哪条历史？

**状态：待回答**

- 最初是 Toyc/Tinylibc 的能力验证，还是独立游戏目标？
- 何时成为仓库开发主线？
- 为什么明确以 GCC 为参考构建，不再要求 Toyc 兼容？
- 它继承了此前哪些底层设施和项目经验？
- 应视为 Toyc 第四阶段，还是从 Toyc 长出的新项目？

落点：`periods/toyc.md`，必要时新增 Rasterfall 转向专题。

## 单题处理流程

1. 原样或忠实摘要记录作者回答，并注明日期。
2. 列出回答中可由仓库验证的时间、文件、提交和行为。
3. 查找反例、分支差异与同期文档，保留不一致之处。
4. 将确认内容写入时期文档，把推断和未知项继续留在台账。
5. 在问题状态改为 `已写入` 时注明目标文档与关键证据。

---

## 文件：`docs/archaeology/sources.md`

# 证据与方法

## 证据等级

按下列层次保存材料，不把不同性质的证据混写成确定事实：

1. **对象级证据**：提交对象、父提交、树、blob、tag、构建产物及可复现命令输出。
2. **仓库内陈述**：提交说明、README、CLAUDE.md、脚本注释和历史文档。
3. **外部同期材料**：GitHub 页面、issue、release、聊天记录、API 日志、账单和本地工作日志。
4. **作者回忆**：作者对动机、工具、过程和感受的口述。
5. **研究者推断**：由若干证据拼出的解释，必须注明推断过程和不确定性。

提交说明属于同期陈述，不等于其宣称的功能已经被独立复现。`Co-Authored-By` 只说明提交元数据
显式署名，不应用来计算某个工具生成了多少代码。

## 本地资料

| 项目 | 本地路径 | 远端 | 本轮观察到的提交范围 |
|---|---|---|---|
| SC7 | `../SC7` | `WHU-SC7/SC7` | `ec37618` 至主分支 `1a668a3`，620 个提交 |
| Tinylibc | `../Tinylibc` | `WHU-SC7/Tinylibc` | `fce216c` 至 `a566206`，211 个提交 |
| ToyCCompiler | `../ToyCCompiler` | 上游公开仓库 | `22ffcc8` 至 `58ac389`，96 个提交 |
| Toyc | `.` | 上游公开仓库 | `22ffcc8` 至本轮 HEAD `cf8c5cd`，773 个提交 |

提交数量和 HEAD 只记录本轮调查现场，后续仓库推进后不作为永久统计结论。

## 常用复查命令

```sh
git -C ../SC7 log --all --reverse --date=iso-strict
git -C ../Tinylibc log --reverse --date=iso-strict
git -C ../ToyCCompiler log --reverse --date=iso-strict
git log --reverse --date=iso-strict
git show --stat --summary <commit>
git diff --no-index <old-tree-path> <new-tree-path>
git log --follow -- <path>
git blame <commit> -- <path>
```

跨仓库比较不能只看文件名。应先固定两端提交，再对目录树做 hash、相似度和语义对照，并记录
重命名、格式化、生成文件与二进制种子等干扰因素。

## 作者口述基线

以下内容来自本计划启动时的作者说明，尚未全部得到外部材料佐证：

- 项目主线为 Tinylibc → ToyCCompiler → Toyc，三个项目存在传承关系。
- SC7 是作者在 2025 年参加操作系统内核比赛时的团队作品。
- Tinylibc 主要属于手写时期；学习依赖网页端对话，也复制过部分代码原型，调试和主要开发由
  作者自己进行。
- ToyCCompiler 时期高强度使用 Claude Code 和 DeepSeek API，并完成自己的 C 编译器。
- Toyc 保存了大部分提交信息；两个历史项目的公开仓库已克隆在当前仓库上级目录。
- 整条 C 项目主线体现了作者逐渐深入计算机底层、从 C 编程提升到 C 项目能力，并逐渐使用
  coding agent 的过程。

## 外部赛程材料

- [2025 年内核实现赛道比赛平台记录](https://course.educg.net/pages/contest/contest.jsp?contestCID=0&contestID=R0LIpo5ERIA&my=false&tabDocID=4681822)：记录初赛截止 2025-06-30、决赛阶段和 2025-08-20 的 QEMU/开发板测试安排。
- [全国大学生操作系统比赛公开信息](https://github.com/oscomp)：记录 2025-06-30 初赛结束和 2025-07-09 决赛名单公布。
- [赛事通知（地方高校转载）](https://cxcy.ysu.edu.cn/info/1043/2752.htm)：给出决赛阶段 1、阶段 2 的日期切分。

后续引用作者口述时应标注访谈日期、问题原文、回答原文或摘要，以及作者是否在事后修订。

## 待收集材料

- 四个 GitHub 仓库的创建时间、默认分支变化、release、tag、issue 和仓库描述历史。
- SC7 各比赛阶段的分支、团队分工，以及 `tlibc` 分支与 Tinylibc 的关系。
- Tinylibc 从 SC7 分离前的代码来源和授权边界。
- 网页端对话的服务、时间范围、保留形式和可公开范围。
- Claude Code 会话、本地配置、命令历史和显式署名策略。
- DeepSeek API 调用脚本、模型版本、提示词、输出与费用记录。
- 未进入 Git 的失败实现、临时文件、截图、构建日志和二进制产物。

---

## 文件：`docs/archaeology/timeline.md`

# 初步时间线

本时间线只列已经由本地仓库提交记录核对的边界事件。提交日期使用提交对象记录的 `+08:00`
时间；历史叙事补充前应继续核对作者日期、提交者日期、分支和文件内容。

| 日期 | 仓库 | 提交 | 事件 | 证据性质 |
|---|---|---|---|---|
| 2025-03-23 | SC7 | `ec37618` | 团队竞赛内核首次提交，说明已支持双架构 UART 输出 | SC7 根提交说明 |
| 2025-08-20 | SC7 | `28aab57` | 决赛现场赛重新适配物理开发板的起点 | SC7 同期文档所列提交 |
| 2025-08-29 | SC7 | `1a668a3` | 当前主分支最后提交 | 提交对象；不代表项目所有分支终点 |
| 2025-09-24 | SC7 | `e2eef7f` | 在 `origin/tlibc` 支线开始独立发展 Tinylibc | 作者提交记录；支线后续仍有提交 |
| 2025-10-17 | Tinylibc | `fce216c` | 从 SC7 项目分离并尝试支持 x86_64 | Tinylibc 根提交说明 |
| 2026-06-30 | Tinylibc | `4670d9f` | 加入最小可行的自举编译器 Phase 1 | 提交内容与说明 |
| 2026-07-01 | Tinylibc | `9921705` | 提交说明记录 `tcc.c` 自编译通过 | 提交说明，能力仍待复现 |
| 2026-07-02 | Tinylibc | `2213ae5` | `tmake -T` 开始以 tcc/tas 替代 gcc | 提交内容与说明 |
| 2026-07-04 | ToyCCompiler / Toyc | `22ffcc8` | 从 Tinylibc 提取 tcc，建立独立仓库 | 两仓共享根提交；提交说明 |
| 2026-07-08 | ToyCCompiler / Toyc | `961fbf6` | 提交说明记录自举到 stage 10 | 提交说明，脚本与产物待复现 |
| 2026-07-08 | ToyCCompiler / Toyc | `8b2ea1f` | 发布“自举成功宣言” | 提交说明与 README 历史 |
| 2026-07-09 | ToyCCompiler / Toyc | `7adcf8f` | 提交说明记录种子、gcc-free 构建和 tas 闭环 | 提交内容与说明 |
| 2026-07-10 | ToyCCompiler / Toyc | `9948ea0` | tld 接管链接，形成工具链自举闭环 | 提交内容与说明 |
| 2026-07-11 | ToyCCompiler | `58ac389` | README 以 HTTP 301 形式宣告迁往 Toyc | 提交内容 |
| 2026-07-11 | Toyc | `643287b` | README 将项目描述为来自 ToyCCompiler 与 Tinylibc 的 “two parents” | 提交内容 |
| 2026-07-11 | Toyc | `a26e7a6` | 编译器源码由 `app/` 移到 `compiler/`，为 Tinylibc 整合留出目录 | 提交内容与说明 |
| 2026-07-23 | Toyc | `5bffca4` | 工具链由 tcc/tas/tld 等统一改名为 toyc/toyas/toyld | 提交内容 |
| 2026-07-24 | Toyc | `af6bc30` | 引入 Tinylibc 的完整库与应用树及 GCC 构建规则 | 提交内容与说明 |

## 尚未定论的边界

- SC7 的 `tlibc` 分支与 Tinylibc 根提交之间是否存在可直接匹配的代码快照。
- Tinylibc 的“手写时期”应从 SC7 前史算起，还是从独立仓库根提交算起。
- ToyCCompiler 根提交对应 Tinylibc 的准确源提交；目前只能确定提取发生在 2026-07-04，且
  Tinylibc 在 2026-07-03 已有接近的编译器树。
- Toyc 在 2026-07-24 引入的 Tinylibc 文件对应上游哪个快照，之后两仓是否发生双向同步。
- Rasterfall 应作为 Toyc 第四时期，还是作为 Toyc 时期内部的第二条项目主线。

