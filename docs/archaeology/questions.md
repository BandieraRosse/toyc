# 考古问题与结论索引

本文档归档本轮考古围绕的核心问题、结论落点和证据边界。Q00—Q10 均已在当前考古范围内完成整理；
本索引不构成后续待办，也不替代时期叙事、提交级证据表或专题文章。保留问题原貌，是为了让未来读者
能够从结论反查当时需要解释的历史关系、作者口述和证据限制。

## Q00：整条主线意味着什么？

**归档状态：已完成当前范围核查**

作者总述：这条 C 语言项目的演进，是逐渐深入理解计算机底层世界、提高 C 语言项目能力
（已不只是 C 编程能力），并逐渐使用 coding agent 的过程。

最终落点：[SC7 与系统工程的边界](essays/sc7-engineering-boundaries.md) 说明早期架构、接口、
内存模型、运行环境和范围决策；[从逐行掌握到风险驱动理解](essays/agent-and-project-scale.md)
跨 Tinylibc、ToyCCompiler、Toyc 与 Rasterfall 总结了项目规模、理解成本、验证边界和 agent
协作方式的共同演进。三条变化互相促进，但 agent 扩大规模也带来局部理解不足和维护复杂度，
不能写成单向进步或某日突然完成的能力跃迁。

## Q01：SC7 中作者实际负责什么？

**归档状态：已完成当前范围核查**

作者已确认，Toyc 与 SC7 中的多个历史提交身份均属于本人。为保护隐私，考古文档不记录具体姓名、
邮箱或账号标识。

稳定叙事已写入 `periods/sc7.md`，逐提交依据与冲突见 `evidence/sc7.md`。当前可确认作者主导了
RISC-V 用户态/异常/系统调用早期框架，并持续参与设备适配、文件相关系统调用和后期 `tlibc`
用户态生态；初始 ext4/lwext4、LoongArch 用户态虚拟化及后续 VMA 主线主要由队友推进。

作者于 2026-09-15 补充：初赛负责原型和大部分底层硬件规范相关工作，后期参与动态链接并分担约
三分之一 BusyBox、libctest 等测试；决赛阶段一分担约三分之一 LTP 任务，并负责上板调试和驱动；
现场赛负责上板与测试，在队友改进动态链接系统调用后进行板端验证，还修复了 U-Boot 启动环境下
BSS 必须主动清零及多处显式初始化的问题。上述比例均为主观工作量估计。

同日补充还确认：多核因锁问题和随机错误、定位成本高且并非强制任务而放弃；SD 驱动的关键初始化、
读取、写入函数及文件由队友提供；作者没有参与文件系统实现，主要使用其接口。LoongArch VirtIO
磁盘另一次协作修复已定位为队友 `92ccdea` 记录忙等待问题、作者 `530de36` 与 `02a1167` 连续修正；
Git 仍不能量化协助方式。

落点：`periods/sc7.md`；证据台账：`evidence/sc7.md`。

## Q02：Tinylibc 怎样从 SC7 分离？

**归档状态：已完成当前范围核查**

作者于 2026-09-15 补充：比赛后仍想继续写 C，但内核难度高、成果周期不明确，因此先转向更简单的
用户态；最初 `tlibc` 仍在 SC7、QEMU 和非 x86_64 架构上运行。后来为了在原生 Ubuntu 和个人
电脑上直接运行、摆脱 QEMU，而迁移整个 SC7 到 x86_64 又过于困难，遂分离 Tinylibc 并逐步转向
x86_64。早期 Tinylibc 仍保留 RISC-V 部分，后来不再使用。作者没有保留 SC7 Git 历史，是因为
SC7 不支持 x86_64，作者当时认为旧架构历史对新的 libc 主线没有保留必要。这是当时的取舍，
不作为那段历史客观上没有技术或考古价值的结论。

原始问题及当前答案：

- `tlibc` 分支何时开始、由谁主要开发：`e2eef7f` 于 2025-09-24 开始独立发展用户态库；主要开发者
  身份由作者口述确认，提交对象只能确认相应身份完成的改动。
- Tinylibc 根提交来自哪个 SC7 状态：来自 `tlibc` 末端 `d2363da` 的
  `user/include/Tinylibc/`。
- 分离时复制和放弃了什么：复制该目录的完整 29 文件子树，没有复制 SC7 内核；其中 26 个 blob
  相同，只修改三个 Markdown。根提交仍只有 RISC-V 实现，x86_64 可运行适配始于 `7586050`。

落点：`periods/sc7.md`、`periods/tinylibc.md` 和 `timeline.md`。

## Q03：Tinylibc 的手写时期如何工作？

**归档状态：已完成当前范围核查**

已将课程与终端应用、首次暂停、pthread/论文实验、网络与构建工具、agent 转型和编译器冲刺写入
`periods/tinylibc.md`，逐提交依据见 `evidence/tinylibc.md`。手写时期主要使用 DeepSeek 网页端
讲解资料、接口和调试输出，作者主要以 printf 和 strace 亲自调试；6 月下旬以后逐渐转为 prompt、
方向判断和让 agent 读写代码。musl 文件级来源、课程材料和未提交音频实验仍列为证据缺口。

## Q04：为什么开始写 C 编译器？

**归档状态：已完成当前范围核查**

写 C 编译器不只是作者的长期愿望。2026-06-29 的同期会话 `ls-712f9fec88e391ac0047bd5c`
已经明确说明：这个想法约一个月前形成，主要目的就是补上 Tinylibc 自举链条缺失的一环，甚至设想
以后摆脱 Makefile；相较再次挑战完整 Linux 内核，编译器是更现实的下一层系统软件目标。同期讨论
还把 Tinylibc 的身份收敛为透明、可读、独立并由实际需求驱动，自举编译器因而是“独立”原则的
自然延伸，不是 agent 临时提出的独立项目方向。

6 月 30 日完整会话 `ls-f602149a47d21b50e15a5c66` 进一步区分了分工：作者提出 self-host、
freestanding Tinylibc、不得依赖 glibc 和最终静态编译项目自身文件等目标与限制；agent 扫描项目
实际 C 特性、构建系统、ELF 和启动代码，并提出阶段划分及具体架构。coding agent 的效率仍是
真正启动的直接条件，但不能据此把目标来源归给 agent。

落点：`periods/tinylibc.md`；证据台账：`evidence/tinylibc.md`。

## Q05：为什么建立独立的 ToyCCompiler 仓库？

**归档状态：已完成当前范围核查**

文件级来源已经写入：`22ffcc8` 以 Tinylibc `87d61e0` 的编译器目录为精确基底。7 月 4 日同期会话
`ls-ae12d0ff80d1bb2004f69072` 直接记录了作者主动建立新 tcc 项目：此时编译器能编译部分 C 文件，
但编译自身仍有问题；随后建立独立 Makefile、最小 `tcc_need.h` 和依赖边界，并继续围绕 self-host
收缩测试。由此可确认独立仓库是作者主动建立的工程边界。

作者 2026-09-15 关于“集中上下文”和“当时已计划日后重新整合”的说明保留为后期回忆；目前没有
7 月 4 日同期会话把前者写成主要拆分原因，也没有证据证明拆分当日已明确计划合回 Tinylibc。

已回答的原始问题包括：为何选择新根、提取对应哪个 Tinylibc 状态，以及独立时实际建立了什么
工程边界。“为何没有保留祖先链”、集中上下文是否为主要动机、拆分当日是否已计划重新整合，均无
独立同期记录，不再由后来的结果倒推。

落点：`periods/toy-c-compiler.md`；证据台账：`evidence/toy-c-compiler.md`。

## Q06：Claude Code 与 DeepSeek API 如何分工？

**归档状态：已完成当前范围核查**

已知口述：ToyCCompiler 时期以 Claude Code 为客户端，但模型请求全部接入 DeepSeek API，并未同时
使用 Claude 模型。作者为节省费用全程使用 DeepSeek-V4-Flash，仅有两天多使用 Pro，其余时间均以
Flash 开发。提交里的 Claude 共同作者署名不能用来判断实际模型；作者当时没有在意该署名，
事后认为它确实容易使观众误以为项目使用了昂贵的 Claude 模型。

Git 进一步确认 `687ed29` 没有 trailer，且只删改 README。作者于 2026-09-15 确认，该次改动是亲手
完成，没有让 AI/agent 修改文件。被删文字是完成自举后因回想过程困难、情绪激动而写；平静后认为
过于抒情，不适合 README，遂手动删除。GLM-5.2 只作为当时讨论迁移想法的背景；最终的
幽默迁移提交表达了作者想要更幽默的方向，不再追究精确模型归因或首次会话映射。

Claude Code 的具体配置、trailer 生成链和 GLM 会话映射不作为本题主要考证目标。

落点：`periods/toy-c-compiler.md`；证据台账：`evidence/toy-c-compiler.md`。更细的 agent 会话映射
不属于本轮核心主线。

## Q07：“自举成功”包含哪些阶段？

**归档状态：已完成当前范围核查**

已区分 Tinylibc 内源码自编译、GCC/GNU 工具辅助的可运行 stage 2、stage 1—10 与 tcc 二进制
收敛、种子 tcc/tas 接管、tld 自链接及默认 Makefile 接管。最终 stage 脚本仍调用系统 `ld`；“仅
make”只表示默认编译/汇编/链接规则由预置三种子承担，仍依赖 Linux、shell、make、宿主命令和种子。

原始问题已经分别落到以下结论：自身源码编译、可运行下一阶段和多 TU 自编译不能合称一个时点；
toyas 与 toyld 接管默认构建发生在不同提交；受跟踪种子的加入、更新和验证可以复查，但更早未提交
种子无法恢复；stage 收敛比较的是 tcc 可执行文件，不等于完整 C 语义正确；“零外部依赖”采用的
是预置三种子接管默认工具链的工程边界，而不是不依赖操作系统、shell、make 和宿主命令。

落点：`periods/toy-c-compiler.md`；证据台账：`evidence/toy-c-compiler.md`。

## Q08：Toyc 为什么重新整合 Tinylibc？

**归档状态：已完成当前范围核查**

6 月 29 日同期会话已确认 compiler 从一开始服务于 Tinylibc self-host；目前可证的最早强
agent-session 重新统一证据则是 2026-07-11：`ls-d1bf96ba7a8f02510e526f28` 中作者明确要求
简介包含两个旧仓库，说明 Toyc 将与 Tinylibc 合并、成为独立生态，tcc 是重要组成部分。这里证明
的是“到 7 月 11 日计划已明确”，不声称思想必然首次诞生于当天。

同日 Git 提交 `643287b` 的 README 确实把项目写成来自 ToyCCompiler 与 Tinylibc 的合并，英文版
写有 “either parent”；因此 “two parents” 仍有同期 Git 证据，即使现有自然 prompt 未发现该
字面措辞。作者认可这份同期表述代表其主要意图：standalone 编译工具链是核心，
Tinylibc 提供库和应用；二者结合后，生态在理论上形成能创造自身并继续创造其他程序的闭环。这是
作者所说的成熟与重大进步，但不等于初始整合时已经由 toyc 构建所有内容。工程上彻底实现未必可行，
后续维护也常为效率使用 GCC。

Git 对照确认，Tinylibc `32436f1` 的 19 个映射工具链文件精确来自 Toyc `0a7800f`；Tinylibc
连续适配后，Toyc `af6bc30` 新增的 108 个文件又全部精确来自 `6a86e5f`。这构成数小时内的双向
选择性移植，不是 Git merge 或完整镜像。直接融合仍有现实困难，开发因而先在 Toyc 中按各 lib
模块建立测试、逐个补齐能力。`af6bc30` 只迁入较易验证的应用；难迁移或意义有限的老应用留在旧仓库，
所以提交说明的 “full app tree” 不能按字面采用。

落点：`periods/toyc.md`；证据台账：`evidence/toyc.md`。

## Q09：何时从 C 编程转向 C 项目能力？

**归档状态：已完成当前范围核查**

6 月 29 日同期会话已经直接记录“AI 产能超过个人理解速度”的危机：Claude 开发太快，作者来不及
理解新增代码，项目增长超过个人即时理解能力；在首次接触的网络领域，甚至缺少判断 agent 输出
质量的既有知识。因此“逐行掌握不可持续”不再只是后来的总结。

作者于 2026-09-16 将问题进一步修正为现实条件下的规模取舍：第一次接触陌生系统软件、缺少经验时，以
网页 AI 辅助、自己实现和测试，大概率只能推进到早期 Tinylibc 的程度；沿用这种方式实现 standalone
C 编译器并达到 ToyCCompiler、Toyc 或当前任务规模并不现实。作者早期已经感觉到该限制，但开发方式
的意义是在使用 coding agent 的过程中逐渐明确的，不能固定成某个突然转变的日期。

改变方式意味着不再要求掌握每一行实现，而非放弃理解。compiler-tests 是最具体的同期案例：从
要求 feature 声明有测试证据，到用小测试建立能力边界、隔离 self-host failure，再到压缩约 94 个
generic tests、只保留 tcc 实际依赖的语言模式。理解单位由每一行实现转为目标、接口、能力边界、
失败模式和验证结果。作者仍掌握总体目标并关注关键技术选择；
必要时进入对应模块，主要借助 agent 解读。对有既往经验的领域，作者会更敏感地审查选择。测试、
任务分解和模块边界则用于约束扩大规模后的风险。

作者确认 agent 产出的规模有时略微超出控制，Rasterfall 中也出现过意料之外的效果；目前认为这在
个人开发中可以接受。完全掌握同样有成本，理解到一定程度后继续覆盖所有细节的边际成本并不划算，
因此选择抓住主要部分、接受有限风险并继续推进。这不把规模等同于能力，也不表示 agent 产出的每个
细节都已成为作者知识。

落点：[从逐行掌握到风险驱动理解](essays/agent-and-project-scale.md)；SC7 的早期范围决策另见
[SC7 与系统工程的边界](essays/sc7-engineering-boundaries.md)。

## Q10：Rasterfall 属于哪条历史？

**归档状态：已完成当前范围核查**

Git 证据表明它的起点兼具两种属性：8 月 4 日的 Wayland 3D/FPS 灰盒同时验证 GCC 与 Toyc，直接
承担真实负载和编译器回归；8 月 5 日已经发展为有独立规则、敌人、HUD 与音频的游戏，8 月 6 日
命名 Rasterfall 并模块化，8 月 9 日形成顶层独立目录。

功能重心从 8 月 5 日起实际转向游戏；`2d7edb7` 于 8 月 31 日将 Rasterfall 移出 Toyc `self-app`
范围，明确复杂游戏实现不再为编译器兼容受限，正常构建以 GCC 为准；`75a10cd` 于 9 月 3 日把根
协作说明正式转向 Rasterfall，是仓库治理层面的主线切换。Linux 版仍继承 Tinylibc 的 freestanding
运行时、syscall、Wayland、输入、软件渲染、音频和线程设施，也延续真实负载、模块测试和可复核
构建的工程经验。

结论：Rasterfall 是从 Toyc 生态能力验证中长出的新项目；它与 Toyc/Tinylibc 有连续代码和仓库
历史，但独立的玩法、资产、跨平台与构建目标使其不宜称为 Toyc 编译器发展的第四阶段。

落点：`periods/toyc.md`；证据台账：`evidence/toyc.md`。

## 归档阅读说明

每题正文保留三类信息：当时需要回答的历史问题、当前可接受的结论，以及结论对应的时期文档和证据
表。文中仍明确区分 Git 可验证事实、同期陈述、作者回忆和研究者推断；“已完成当前范围核查”不表示
所有旁支材料都已取得，也不表示作者回忆均已被独立复现。新增材料若改变核心结论，可在对应时期文档、
证据表和本索引中修订；仅增加旁支细节时，不必重新开启本索引中的问题。
